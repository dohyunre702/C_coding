#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
int main(void)
{
	int a, b;

	scanf("%d %d", &a, &b);

	for (int i = a; i <= b; i++)
	{
		if (i % 5 == 0 && i % 11 == 0)
			printf("%s\n", "FizzBuzz");
		else if (i % 5 == 0)
			printf("%s\n", "Fizz");
		else if (i % 11 == 0)
			printf("%s\n", "Buzz");
		else printf("%d\n", i);
	}

	return 0;
}
*/