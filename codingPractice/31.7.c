#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
int main()
{
    int num1;

    scanf("%d", &num1);

    for (int i = 0; i < num1; i++)
    {
        for (int j = 0; j < num1 - i; j++)
            printf(" "); //전체를 공백으로
        for (int j = 1; j <= 2*i+1; j++)
            printf("%s", "*"); //해당 파트에만 *
        printf("\n");
    }

    return 0;
}
*/