#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
int main(void) 
{
    int balance = 10000;    // 교통카드 잔액
    int age;

    scanf("%d", &age);

    if (age >= 7 && age <= 12)
    {
        balance = balance - 450;
    }
    else if (age >= 13 && age <= 18)
    {
        balance = balance - 720;
    }
    else
    {
        balance = balance - 1200;
    }

    printf("%d\n", balance);

    return 0;
}
*/