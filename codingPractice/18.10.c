#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
int main(void)
{
	int a, b, c, d;
	//차례로 국어, 영어, 수학, 과학

	scanf("%d %d %d %d", &a, &b, &c, &d);
	
	if ((a >= 0 && b >= 0 && c >= 0 && d >= 0)
		&& (a <= 100 && b <= 100 && c <= 100 && d <= 100))
	{
		int avg = (a + b + c + d) / 4;
		if (avg >= 85)
			printf("%s", "합격");
		else
			printf("%s", "불합격");
	}
	else printf("%s", "잘못된 점수");
	return 0;
}
*/