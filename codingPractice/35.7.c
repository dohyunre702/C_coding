#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    int num1;
    int num2;

    int* numPtr1 = malloc(sizeof(int)); //포인터 numPtr1에 메모리 동적 할당
    int* numPtr2 = malloc(sizeof(int)); //포인터 numPtr2에 메모리 동적 할당

    scanf("%d %d", &num1, &num2);

    *numPtr1 = num1; //포인터 numPtr1을 역참조해 num1 저장
    *numPtr2 = num2; //포인터 numPtr2을 역참조해 num2 저장

    printf("%d\n", *numPtr1 + *numPtr2);

    free(numPtr1);
    free(numPtr2);

    return 0;
}
*/